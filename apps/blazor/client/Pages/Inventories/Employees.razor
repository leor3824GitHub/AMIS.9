@page "/inventories/employees"

<PageHeader Title="Employees" Header="Employees" SubHeader="Manage your Employees." />

<EntityTable @ref="_table" TEntity="EmployeeResponse" TId="Guid" TRequest="EmployeeViewModel" Context="@Context">

    <AdvancedSearchContent>
        <!-- Add any advanced search content here if needed -->
    </AdvancedSearchContent>

    <EditFormContent>
        @if (!context.IsCreate)
        {
            <MudItem xs="12" md="6">
                <MudTextField Value="@context.RequestModel.Id" Underline=false ReadOnly Label="Employee Id" />
            </MudItem>
        }
        <MudItem xs="12" md="6">
            <MudTextField Label="Name" For="@(() => context.RequestModel.Name)" @bind-Value="context.RequestModel.Name" />
        </MudItem>
        <MudItem xs="12" md="6">
            <MudTextField Label="Designation" For="@(() => context.RequestModel.Designation)" @bind-Value="context.RequestModel.Designation" />
        </MudItem>
        <MudItem xs="12" md="6">
            <MudTextField Label="ResponsibilityCode" For="@(() => context.RequestModel.ResponsibilityCode)" @bind-Value="context.RequestModel.ResponsibilityCode" />
        </MudItem>
        <MudItem xs="12" md="6">
            <MudTextField Label="UserId" For="@(() => context.RequestModel.UserId)" @bind-Value="context.RequestModel.UserId" />
        </MudItem>
        @* <MudItem xs="12" md="12">
            <MudItem>
                <div>
                    @if(!context.IsCreate)
                    {
                        <MudButton HtmlTag="label" Variant="Variant.Filled" Color="Color.Info" Size="Size.Small" Target="_blank"
                            StartIcon="@Icons.Material.Filled.RemoveRedEye" Style="margin-left: 5px;" >
                            View
                        </MudButton>

                        <MudButton HtmlTag="label" Variant="Variant.Filled" Color="Color.Error" Size="Size.Small"
                                   StartIcon="@Icons.Material.Filled.Delete" Style="margin-left: 5px;">
                            Delete
                        </MudButton>
                    }
                </div>
            </MudItem>
        </MudItem> *@
    </EditFormContent>

</EntityTable>

